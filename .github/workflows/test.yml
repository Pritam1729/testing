name: tester

on:
  workflow_dispatch

jobs: 
  test:
      runs-on: ubuntu:latest
      steps:
        - name: this should fail
          id: failed
          run: exit 1
        - name: this should not fail
          id: success
          run: exit 0
